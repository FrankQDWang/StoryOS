# Deferred Eval Observation Boundary

- Status: deferred; outside the MVP.
- Contract revision: `eval-mvp-boundary-2026-08-29-v1`.
- Owner: [Record the Deferred Eval Observation Boundary](https://github.com/FrankQDWang/StoryOS/issues/61).
- Canonical glossary: [CONTEXT.md](../../CONTEXT.md).

## Current boundary

Eval is outside the MVP. The future concept is a separate Web page outside
the main writing interface, with separate APIs. Its purpose is to observe
the Agent input and output at each step, including Text and Function Calling
output, Trace, token usage, and elapsed time.

No Eval design or implementation is authorized now. This note selects no
page layout, interaction, API, schema, capture pipeline, or assessment method.
Eval creates no MVP delivery, implementation-ticket, or release requirement.

## Existing records and earlier text

Normal Agent Run, input/output, Trace, usage, and timing records retain their
existing requirements. Their owning runtime, Artifact, privacy, permission,
and recovery contracts remain unchanged. This note adds no capture,
retention, or verification work for a future Eval surface.

The user's 2026-08-29 clarification replaces the former Eval evidence
specification. Its case, corpus, assessment, evaluator, score, baseline,
comparison, and feedback designs are not current product requirements.
Git history retains that earlier text as history, not an execution input.

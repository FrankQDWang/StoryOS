apiVersion: 1
datasources:
  - name: Verification observation
    uid: storyos-verification
    type: frser-sqlite-datasource
    access: proxy
    editable: false
    jsonData:
      path: /observation/runs.sqlite
      pathOptions: "mode=ro&_pragma=busy_timeout(5000)"
      attachLimit: 0

---
description: Use the repository file-selection policy after edits and test lifecycle changes.
alwaysApply: true
---

# Verification selection

Start with `make verify-status BASE=origin/main`. Follow `docs/agents/verification.md`
for ordinary edits, test lifecycle changes, candidate reviews, and failed complete
runs. Keep all command and policy details in that common guide.
